# Public configuration only. Copy to .env.local; do not put service_role or secret keys here.
# Leave disabled until migrations, invite-only auth and isolation tests pass on Supabase.
PORTAL_ENABLED=false
PORTAL_SUPABASE_URL=
PORTAL_SUPABASE_PUBLISHABLE_KEY=
